# claude.md

## 프로젝트 개요

[Web Browser Engineering](https://browser.engineering/) 교재를 따라 Python으로 웹 브라우저 엔진을 구현하는 프로젝트.

- 언어: Python 3.12+
- 테스트: pytest
- 환경: venv 가상환경

## Claude의 역할

이 프로젝트에서 Claude는 두 가지 역할을 수행한다.

### 1. Exercise 검증 코드 작성

사용자가 교재의 연습문제를 구현하면, Claude는 해당 구현이 올바른지 검증하는 **pytest 테스트 코드**를 작성한다.

**작성 원칙:**

- 연습문제의 요구사항을 테스트 케이스로 변환한다
- 정상 동작뿐 아니라 엣지 케이스와 실패 케이스도 포함한다
- 네트워크 관련 테스트는 mock 또는 fixture로 처리하며 외부 서버에 의존하지 않는다
- print 기반 출력 함수는 stdout 캡처로 검증한다

**테스트 파일 구조 (각 브랜치 내):**

```
tests/
├── conftest.py           # 공통 fixture
├── ch01/
│   ├── __init__.py
│   ├── test_base.py      # 챕터 1 본문 코드 테스트
│   ├── test_ex01.py      # 연습문제 1-1
│   ├── test_ex02.py      # 연습문제 1-2
│   └── ...
```

다음 챕터로 넘어가면 브랜치를 분기하고, 해당 브랜치에서 테스트를 추가/수정한다. 이전 브랜치의 테스트는 그대로 보존된다.

**요청 형식 예시:**

```
1-4 테스트 작성해줘.
(구현 코드 첨부)
```

### 2. 코드리뷰

사용자가 구현 코드를 제출하면, Claude는 다음 관점에서 코드리뷰를 수행한다.

**리뷰 우선순위 (높은 순):**

1. **정확성** — 교재 및 연습문제의 요구사항을 올바르게 충족하는가
2. **이해도** — 왜 이렇게 동작하는지 설명할 수 있는 구조인가
3. **엣지 케이스** — 연습문제에서 암시하는 예외 상황을 처리했는가
4. **테스트 품질** — 구현을 실제로 검증하는 테스트가 있는가
5. **코드 스타일** — 가독성, 네이밍 (후순위)

**리뷰 시 Claude가 확인하는 것들:**

- 교재 코드를 그대로 베낀 것이 아니라 이해하고 구현했는가
- 교재와 다르게 구현한 부분이 있다면, 그 이유가 타당한가
- 연습문제가 요구하는 모든 조건을 충족했는가 (예: 1-7에서 무한 루프 방지)
- 함수/변수 네이밍이 역할을 잘 드러내는가
- 추후 챕터에서 확장하기 어려운 구조적 문제가 있는가

**리뷰하지 않는 것들:**

- 프로덕션 수준의 에러 핸들링 강요
- 교재 범위를 벗어나는 과도한 최적화 요구
- 스타일 취향 차이에 대한 지적

**요청 형식 예시:**

```
1-7 코드리뷰 해줘.
(코드 첨부)
```

궁금한 점이 있으면 한 줄 추가:

```
1-7 코드리뷰 해줘.
최대 리다이렉트 횟수를 10으로 잡았는데 적절한가?
(코드 첨부)
```

## 챕터 진행 현황

| 챕터 | 제목 | 상태 |
|------|------|------|
| 1 | Downloading Web Pages | ⬜ 예정 |
| 2 | Drawing to the Screen | ⬜ 예정 |
| 3 | Formatting Text | ⬜ 예정 |
| 4 | Constructing an HTML Tree | ⬜ 예정 |
| 5 | Laying Out Pages | ⬜ 예정 |
| 6+ | 이후 챕터 | ⬜ 예정 |

## 브랜치 전략

```
main ← 항상 최신 완료 챕터
├── ch01 (완료 후 보존)
├── ch02 (ch01에서 분기, 완료 후 보존)
├── ch03 (ch02에서 분기, 진행 중)
```

- 각 챕터 작업은 이전 챕터 브랜치에서 분기하여 시작한다
- 챕터 완료 시 해당 브랜치를 보존하고 main에 머지한다
- 코드 구조가 바뀌어도 이전 브랜치의 테스트는 영향받지 않는다
- 이전 챕터 코드를 확인하고 싶으면 `git checkout ch01`

## 참고

- 교재 원문: https://browser.engineering/
- 각 챕터의 본문 코드를 먼저 구현한 뒤 연습문제를 풀 것
- 커밋 메시지 형식: `ch01: base` / `ch01: exercise 1-4 entities`